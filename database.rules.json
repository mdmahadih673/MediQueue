{
  "rules": {
    "users": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'",
      "$uid": {
        ".read": "auth != null && (auth.uid == $uid || root.child('users').child(auth.uid).child('role').val() == 'admin')",
        ".write": "auth != null && auth.uid == $uid",
        ".validate": "newData.hasChildren(['uid', 'name', 'email', 'role', 'lastSeen']) && newData.child('uid').val() == auth.uid && newData.child('email').val() == auth.token.email && ((auth.token.email == 'mdmahadih673@gmail.com' && newData.child('role').val() == 'admin') || (auth.token.email != 'mdmahadih673@gmail.com' && newData.child('role').val() == 'user'))"
      }
    }
  }
}
